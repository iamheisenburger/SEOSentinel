# Run-scoped activation35 — local review only

Assignment `supervisor-20260914-slc-scoped-activation-35`, based on
`5b601ad5bb30bb9e18bc906b49029ecf3ca575fb`. The final handoff supplies the exact
new candidate commit. No35 push, deployment, production query/mutation, migration,
grant attachment, pricing configuration, provider call or financial increase.
Provider expenditure in35 is USD0. Production remains the runtime40c4d90 released
in34; see `PENTRA_SLC_DORMANT_RELEASE_2026-09-14.md` for its actual receipts.

## Authority and scope

The original September14 owner approval of USD20 TOTAL ADDITIONAL and the
approved canonical SLC plan already authorize migration of the exact Pentra and
LeadPilot sites and a separately tracked finite validation allowance. No second
funding or migration approval is required. Distinct audit references below point
to that SAME original approval, not a new or inferred approval. The supervisor
explicitly corrected the redundant extra-approval prerequisite in35. Historical
handoffs' contrary prerequisites are superseded by this current clarification.

The USD20 run includes accelerated tests, ordinary cycles, replenishment and
measured follow-up; it cannot renew or grow. Historical USD4 discovery, ordinary
account32/fleet35 limits and their retained reservations remain unchanged.
Additional scoped costs do not consume or enlarge ordinary headroom. This is the
already-approved separate allowance, not a release of old unknown charges.

This assignment ends at independent code review. The following activation is
prepared, not executed. A CLI operator migration is not evidence of successful
customer login, onboarding or signed-in desktop/mobile acceptance.

## Defect and smallest repair

The deployed parser ignored unknown properties in global
`PENTRA_CONTENT_WORK_PRICING`. Adding a run ID to that JSON did not scope it:
unrelated sites could still admit new work. Reproduced on an isolated archive of
exact5b601ad in `/tmp/pentra35-repro.fcJd0u`, using the identical current test and
the archived registered handlers:

```sh
node --experimental-strip-types --test --test-name-pattern='SLC35 scoped pricing enables only' /Users/madmanhakim/Desktop/SEOSentinel-managed-integrated/.claude/assignment24/tests/core-pipeline-integration.test.ts
```

From the archive working directory: 0 passed/1 failed,255.885333ms. The unrelated
site actually returned `buffer_fill`, expected `content_pricing_unavailable`.
This is a reproduced admission defect, not fixture setup failure or paid I/O.

The existing JSON now optionally accepts `validationAuthorizationId`. New
admission and every fresh provider call resolve the saved immutable run through
the existing site/job/reservation lineage. Missing, malformed, mismatched,
unbound, foreign, corrupt, stopped or expired scope fails closed. There are no
tenant IDs in the runtime selector and no new caller flags, routes, tables,
indexes, queues or ledgers. One additive optional field pins the selector to the
existing job pricing snapshot. Configuration changes cannot reprice existing
jobs or retroactively adopt ordinary work into this run.

Unscoped deployment behavior remains unchanged. Under scoped deployment,
unrelated existing jobs cannot make fresh calls either; original reservations
remain. Removing the selector/configuration does not turn scoped jobs ordinary.
Cached results, in-flight known-cost settlement and reconciliation remain usable.
Prepared scoped work can activate its first window and deliver after funding
stop/expiry/removal, with valid retained lineage and all original artifact,
quality, entitlement, consent and destination checks. No model call is needed
for that delivery. Refill still requires active scoped authority and funding.

Settings disclose `validation_run`, `ordinary` or `unavailable`, without leaking
run IDs, approval references or credentials. They explicitly distinguish model
execution from already-prepared delivery and internal headroom from unverified
provider credit.

## Connected local evidence

Fifteen new test results, including three nested lifecycle cases, cover:

- Both exact synthetic sites versus unrelated same-owner/foreign sites, and
  ordinary jobs reserved before scoped configuration.
- Missing/corrupt selector, anchor, ownership, schedule, job, snapshot and receipt;
  stopped/expired scope; readiness and wrong-actor rejection.
- Immutable original rates/model/budget; concurrent reservations; repeated
  admission; restart and September-to-October cumulative20 retention despite
  ordinary capacity; unknown charges retained; known-cost reservation renewal.
- Four paid-for ready items delivering across two fixed windows after stop,
  expiry or pricing removal, before either first schedule had activated. Zero
  extra model calls, jobs or reservation mutations; fresh refill blocked.
- In-flight completion, duplicate settlement and cached responses after shutdown;
  ordinary snapshot behavior when unscoped pricing is removed.

The retained32/33 cases also exercise cancellation before/after I/O, retry and
OCC handling, immutable attachment, finite quality revisions/replacement, receipt
omission, deleted-account tombstones and lifecycle accounting. All use actual
registered handlers; database/clock/network are synthetic and offline.

Both scoped business fixtures complete three fresh discovery/create/review/
publish/verify/refill cycles with old account32/discovery4/fleet35 fully occupied.
Ten fresh jobs,30 mocked model calls,6000 synthetic microUSD,2 ready items per
site remaining. Historical ordinary receipt rows and unrelated headroom remain
identical. Exact synthetic timestamps on 2026-09-11 UTC:

| Cycle | Fixed deadline | ReservoirNote publication | CedarCare publication | Ready after refill, each |
| --- | --- | --- | --- | --- |
| 1 | 12:10:00.000 | 12:05:00.012 | 12:05:00.013 | 2 |
| 2 | 12:40:00.000 | 12:35:00.055 | 12:35:00.056 | 2 |
| 3 | 13:10:00.000 | 13:05:00.031 | 13:05:00.032 | 2 |

These are not Pentra/LeadPilot production articles, real billing or SEO growth.

Final free gates:

- Full repository:1689 tests,1688 passed,0 failed,1 existing skip;116951.804959ms.
- Focused32/33/35:45 passed,0 failed,0 skipped;11625.889375ms.
- Browser:28 passed,2 genuine authenticated skips;4.9s. Desktop/mobile synthetic
  component scope disclosures pass; final screenshots visually inspected.
- Typecheck/build passed. Lint0 errors/157 existing warnings. Schema compatibility
  against5b601ad passed:61 tables,296 indexes, no deleted contracts.
- Secret scan passed on684 tracked files; full dependency audit0 vulnerabilities.
  Staged whitespace check passed.

The full suite ran before the final JSX apostrophe entity escape; final types,
focused tests, build, lint and browsers ran after that identical-rendering fix.
No real owner browser was inspected or operated in35. The two auth skips remain
acceptance gaps, not assumed success from admin access.

## Bounded model pricing for the following activation

Official first-party documentation checked September14: `claude-sonnet-5` is the
API model ID ([models](https://platform.claude.com/docs/en/models/overview)); base
input is USD2/million tokens and output USD10/million, now standard rather than
an expired introductory rate ([pricing](https://platform.claude.com/docs/en/about-claude/pricing)).
The existing Messages path uses client-side structured tools, no optional paid
research/images, cache writes, server tools, thinking, regional premium or SDK
retries. Provider wallet balance and model access have not been probed.

Proposed per-item reservation: USD2.50 (2500000 microUSD), within the SAME total20.
The existing hard input/output bounds price any single call at no more than
USD0.563840. The recorded12-call worst-quality fixture's prompt/output ceilings
reprice to USD2.045316/item; this is an illustration, not an expected live bill.
The absolute12-call ceiling is USD6.766080, so2.50 intentionally stops before a
worst-case path can overspend. Quality never becomes optional to fit the cap.

At2.50 maximum spend each,20 covers at most8 items; the planned accelerated plus
three ordinary cycles and refill needs12 items before measured follow-up. Actual
verified costs may be lower and release unused reserved capacity. Completion
within20 is NOT guaranteed. Unknown costs retain the original ceiling. If a next
required item/call cannot fit, stop fresh I/O, report exact spent/held/remaining,
and continue only already-funded delivery/reconciliation. Never renew/increase.

## Exact following activation sequence — not executed in35

1. Independently accept this candidate, then deploy exact reviewed code and
   verify hosted gates. Keep `PENTRA_CONTENT_WORK_PRICING` absent throughout both
   schedule selections and binding. Do not enable unscoped pricing temporarily.
2. Refresh credential-free projections only for Pentra
   `jh74txye54jna4t85m6y7p4d6h82v9ab` and LeadPilot
   `jh7cccny67df67rdm4jp65tmtn8am982`. Resolve the owner subject from these two
   exact site records only; require both match. Resolve the existing entitlement,
   monthly authorization ID/reference, cadence, domain revision, confirmed facts,
   publisher destination/generation, readiness and pending work. Never enumerate
   other sites/tenants or print raw tokens/configuration.
3. Use the installed Convex CLI's existing admin-authenticated `run --prod
   --identity <JSON>` capability. The JSON must contain `subject` resolved in2;
   never omit it and accept the CLI's generated fallback. The installed
   `node_modules/convex/src/cli/lib/run.ts` constructs this identity. This does
   not authenticate the customer browser or authorize choosing a Google identity.
4. For each exact site, call owner-checked `contentWork:readiness` to obtain its
   current `reviewToken`; then call the existing `contentWork:selectServiceMode`
   with that identity, exact siteId, `mode: growth_first`,
   `confirmBusinessProfile: true`, `authorizeAutomaticPublication: true`, current
   reviewToken and existing timezone. Preserve confirmed facts, destinations and
   all unselected pages. Preserve ordinary cadence using
   `floor(604800000 / site.cadencePerWeek)` (existing `cadenceIntervalMs`). Do not
   accelerate that interval. Existing nonterminal legacy work must reconcile
   through its normal handlers if selection rejects; never reset attempts.
5. Set a deliberate NEW first controlled test deadline, for example the recorded
   activation start plus60 minutes, always at least5 minutes in the future at
   mutation time. Record its exact UTC value. Keep the old missed deadlines as
   history; do not relabel the new test window as an on-time legacy delivery.
   Both sites must be safely selected while still unpriced; if either rejects,
   leave pricing absent. Do not enable spending for only a partially bound run.
6. Call existing `providerBudget:attachCumulativeValidationBudget` with the two
   exact site IDs, resolved existing authorizationId and
   `expectedMonthlyApprovalReference`, `limitMicroUsd: 20000000`, stable run
   reference `owner-20260914-slc-validation-run` and
   `independentFunding: {scope: additional_provider_allowance,
   approvalReference: owner-20260914-slc-additional-provider-20}`. Both references
   document the same original approval; confirm they differ from the existing
   monthly reference. Omit an invented expiry. Attachment must be idempotent;
   never replace/renew an existing run or monthly approval to force it through.
7. Verify both schedule bindings, exact immutable total/sites/approval contract
   and unconsumed run before enabling the one existing configuration:

   ```json
   {"model":"claude-sonnet-5","inputMicroUsdPerToken":2,"outputMicroUsdPerToken":10,"budgetMicroUsd":2500000,"validationAuthorizationId":"<exact-bound-authorization-id>"}
   ```

8. Let the existing engine perform preparation and saved natural window wakes.
   Verify current readiness scope, before-I/O reservations, actual quality,
   exact new live artifacts and fresh refill after each consumption on both
   sites. Retain exact deadlines, publication times, ready count and failures.
   Recheck run spent/held/remaining separately from unchanged ordinary limits.
   No optional paid services or discretionary work outside this approved run.
9. Report operator migration separately from genuine signed-in acceptance.
   Three ordinary cycles at unchanged cadence and actual measured follow-up
   cannot be proved by a same-day accelerated fixture. Fourteen days applies
   BETWEEN discretionary revisions of the same page (`lastImprovedAt`), not
   after creation or before its first measured improvement. After the new
   first deadline, derive each next deadline by the preserved interval; never
   move a failed window. Do not promise same-day full acceptance or attributable
   SEO growth. No backlinks or prospect contact.

## Production failures still open (historical34 observations, not refreshed35)

`provider_account_monthly_budget_reserved` is Pentra's internal account-month
guard, not proof of provider credit exhaustion. Two authorized sites accounted
for USD31.042560: verified actual2.992560, settled conservative ceilings12.000000
and outstanding held16.050000. Account32 headroom is at most0.957440; old4 window
consumed3.121440, headroom at most0.878560. These are upper bounds because no
other tenant was inspected; fleet35 was not enumerated. Reset is
2026-10-01 00:00:00 UTC. Narrow audits found no invalid/orphan/duplicate/amount-
mismatch or expired-active hold defect justifying release. Provider credit is
unverified. This assignment repairs safe activation, not that valid old ledger.

| Tenant | Legacy buffer | Last actual publication UTC | Original missed deadline UTC |
| --- | --- | --- | --- |
| Pentra | 0/4 | 2026-09-12 10:24:14.649 | 2026-09-13 10:24:14.649 |
| LeadPilot | 0/12 | 2026-09-07 22:15:34.409 | 2026-09-08 06:15:34.409 |

In34 both remained legacy/unbound/unpriced, independent consumption0. Same GitHub
destinations verified at2026-09-14 20:23:23.499/20:23:25.221 UTC respectively.
Those receipts are valid for72 hours only while their exact configurations remain
unchanged. Fresh production discovery/generation/quality/live delivery/refill and
genuine owner-browser acceptance remain unproved. Pentra is not declared ready
for monetisation by this local candidate.
